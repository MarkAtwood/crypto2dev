/* SPDX-License-Identifier: GPL-2.0-only */
/*
 * hmac.h — HMAC-SHA-256, HMAC-SHA-384, and HMAC-SHA-512 known-answer test vectors
 *
 * Source: RFC 4231, §4.2–4.4 (Test Cases 1–3)
 *   https://www.rfc-editor.org/rfc/rfc4231
 *
 * Verified against:
 *   - HMAC-SHA-256: gogood-wolfssl wolfcrypt/hmac/hmac_test.go
 *   - HMAC-SHA-384: golang-wolfssl wolfssl-src/wolfcrypt/test/test.c hmac_sha384_test()
 *   - HMAC-SHA-512: gogood-wolfssl wolfcrypt/hmac/hmac_test.go
 * Do NOT modify these values. Do NOT generate vectors from the code under test.
 */
#ifndef CRYPTO2DEV_VECTORS_HMAC_H
#define CRYPTO2DEV_VECTORS_HMAC_H

struct hmac_vector {
	const unsigned char *key;
	unsigned int         key_len;
	const unsigned char *input;
	unsigned int         input_len;
	const unsigned char *tag;        /* HMAC output */
	unsigned int         tag_len;    /* digest size in bytes */
	const char          *source;
	/* fips_skip=1: key is below FIPS 140-3 minimum length — skip when fips_enabled */
	int                  fips_skip;
};

/* ── Shared input strings ───────────────────────────────────────────────── */

static const unsigned char hmac_input_hi_there[]            = "Hi There";
static const unsigned char hmac_input_jefe_what[]           =
	"what do ya want for nothing?";

/* 50-byte repeated-byte message for RFC 4231 §4.4 (TC3) */
static const unsigned char hmac_input_dd50[] = {
	0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd,
	0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd,
	0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd,
	0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd,
	0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd,
	0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd, 0xdd,
	0xdd, 0xdd,
};

/* ── Shared key blocks ──────────────────────────────────────────────────── */

/* RFC 4231 §4.2 TC1 key: 20 × 0x0b */
static const unsigned char hmac_key_0b20[] = {
	0x0b, 0x0b, 0x0b, 0x0b, 0x0b, 0x0b, 0x0b, 0x0b,
	0x0b, 0x0b, 0x0b, 0x0b, 0x0b, 0x0b, 0x0b, 0x0b,
	0x0b, 0x0b, 0x0b, 0x0b,
};

/* RFC 4231 §4.3 TC2 key: "Jefe" (ASCII) */
static const unsigned char hmac_key_jefe[]          = "Jefe";

/* RFC 4231 §4.4 TC3 key: 20 × 0xAA */
static const unsigned char hmac_key_aa20[] = {
	0xaa, 0xaa, 0xaa, 0xaa, 0xaa, 0xaa, 0xaa, 0xaa,
	0xaa, 0xaa, 0xaa, 0xaa, 0xaa, 0xaa, 0xaa, 0xaa,
	0xaa, 0xaa, 0xaa, 0xaa,
};

/* ── HMAC-SHA-256 tags ───────────────────────────────────────────────────── */

static const unsigned char hmac_sha256_tc1_tag[] = {
	0xb0, 0x34, 0x4c, 0x61, 0xd8, 0xdb, 0x38, 0x53,
	0x5c, 0xa8, 0xaf, 0xce, 0xaf, 0x0b, 0xf1, 0x2b,
	0x88, 0x1d, 0xc2, 0x00, 0xc9, 0x83, 0x3d, 0xa7,
	0x26, 0xe9, 0x37, 0x6c, 0x2e, 0x32, 0xcf, 0xf7,
};

static const unsigned char hmac_sha256_tc2_tag[] = {
	0x5b, 0xdc, 0xc1, 0x46, 0xbf, 0x60, 0x75, 0x4e,
	0x6a, 0x04, 0x24, 0x26, 0x08, 0x95, 0x75, 0xc7,
	0x5a, 0x00, 0x3f, 0x08, 0x9d, 0x27, 0x39, 0x83,
	0x9d, 0xec, 0x58, 0xb9, 0x64, 0xec, 0x38, 0x43,
};

/* ── HMAC-SHA-384 tags ───────────────────────────────────────────────────── */

static const unsigned char hmac_sha384_tc1_tag[] = {
	0xaf, 0xd0, 0x39, 0x44, 0xd8, 0x48, 0x95, 0x62,
	0x6b, 0x08, 0x25, 0xf4, 0xab, 0x46, 0x90, 0x7f,
	0x15, 0xf9, 0xda, 0xdb, 0xe4, 0x10, 0x1e, 0xc6,
	0x82, 0xaa, 0x03, 0x4c, 0x7c, 0xeb, 0xc5, 0x9c,
	0xfa, 0xea, 0x9e, 0xa9, 0x07, 0x6e, 0xde, 0x7f,
	0x4a, 0xf1, 0x52, 0xe8, 0xb2, 0xfa, 0x9c, 0xb6,
};

static const unsigned char hmac_sha384_tc2_tag[] = {
	0xaf, 0x45, 0xd2, 0xe3, 0x76, 0x48, 0x40, 0x31,
	0x61, 0x7f, 0x78, 0xd2, 0xb5, 0x8a, 0x6b, 0x1b,
	0x9c, 0x7e, 0xf4, 0x64, 0xf5, 0xa0, 0x1b, 0x47,
	0xe4, 0x2e, 0xc3, 0x73, 0x63, 0x22, 0x44, 0x5e,
	0x8e, 0x22, 0x40, 0xca, 0x5e, 0x69, 0xe2, 0xc7,
	0x8b, 0x32, 0x39, 0xec, 0xfa, 0xb2, 0x16, 0x49,
};

static const unsigned char hmac_sha384_tc3_tag[] = {
	0x88, 0x06, 0x26, 0x08, 0xd3, 0xe6, 0xad, 0x8a,
	0x0a, 0xa2, 0xac, 0xe0, 0x14, 0xc8, 0xa8, 0x6f,
	0x0a, 0xa6, 0x35, 0xd9, 0x47, 0xac, 0x9f, 0xeb,
	0xe8, 0x3e, 0xf4, 0xe5, 0x59, 0x66, 0x14, 0x4b,
	0x2a, 0x5a, 0xb3, 0x9d, 0xc1, 0x38, 0x14, 0xb9,
	0x4e, 0x3a, 0xb6, 0xe1, 0x01, 0xa3, 0x4f, 0x27,
};

/* ── HMAC-SHA-512 tags ───────────────────────────────────────────────────── */

static const unsigned char hmac_sha512_tc1_tag[] = {
	0x87, 0xaa, 0x7c, 0xde, 0xa5, 0xef, 0x61, 0x9d,
	0x4f, 0xf0, 0xb4, 0x24, 0x1a, 0x1d, 0x6c, 0xb0,
	0x23, 0x79, 0xf4, 0xe2, 0xce, 0x4e, 0xc2, 0x78,
	0x7a, 0xd0, 0xb3, 0x05, 0x45, 0xe1, 0x7c, 0xde,
	0xda, 0xa8, 0x33, 0xb7, 0xd6, 0xb8, 0xa7, 0x02,
	0x03, 0x8b, 0x27, 0x4e, 0xae, 0xa3, 0xf4, 0xe4,
	0xbe, 0x9d, 0x91, 0x4e, 0xeb, 0x61, 0xf1, 0x70,
	0x2e, 0x69, 0x6c, 0x20, 0x3a, 0x12, 0x68, 0x54,
};

static const unsigned char hmac_sha512_tc2_tag[] = {
	0x16, 0x4b, 0x7a, 0x7b, 0xfc, 0xf8, 0x19, 0xe2,
	0xe3, 0x95, 0xfb, 0xe7, 0x3b, 0x56, 0xe0, 0xa3,
	0x87, 0xbd, 0x64, 0x22, 0x2e, 0x83, 0x1f, 0xd6,
	0x10, 0x27, 0x0c, 0xd7, 0xea, 0x25, 0x05, 0x54,
	0x97, 0x58, 0xbf, 0x75, 0xc0, 0x5a, 0x99, 0x4a,
	0x6d, 0x03, 0x4f, 0x65, 0xf8, 0xf0, 0xe6, 0xfd,
	0xca, 0xea, 0xb1, 0xa3, 0x4d, 0x4a, 0x6b, 0x4b,
	0x63, 0x6e, 0x07, 0x0a, 0x38, 0xbc, 0xe7, 0x37,
};

/* ── HMAC-SHA-256 vector table ───────────────────────────────────────────── */

static const struct hmac_vector hmac_sha256_vectors[] = {
	{
		/* RFC 4231 §4.2 — TC1 */
		.key       = hmac_key_0b20,
		.key_len   = 20,
		.input     = hmac_input_hi_there,
		.input_len = 8,
		.tag       = hmac_sha256_tc1_tag,
		.tag_len   = 32,
		.source    = "RFC 4231 §4.2 TC1: HMAC-SHA-256(0b×20, \"Hi There\")",
	},
	{
		/* RFC 4231 §4.3 — TC2: key is 4 bytes, below FIPS 140-3 minimum */
		.key       = hmac_key_jefe,
		.key_len   = 4,
		.input     = hmac_input_jefe_what,
		.input_len = 28,
		.tag       = hmac_sha256_tc2_tag,
		.tag_len   = 32,
		.source    = "RFC 4231 §4.3 TC2: HMAC-SHA-256(\"Jefe\", \"what do ya want for nothing?\")",
		.fips_skip = 1,
	},
};

#define HMAC_SHA256_VECTOR_COUNT \
	((int)(sizeof(hmac_sha256_vectors) / sizeof(hmac_sha256_vectors[0])))

/* ── HMAC-SHA-384 vector table ───────────────────────────────────────────── */

static const struct hmac_vector hmac_sha384_vectors[] = {
	{
		/* RFC 4231 §4.2 — TC1 */
		.key       = hmac_key_0b20,
		.key_len   = 20,
		.input     = hmac_input_hi_there,
		.input_len = 8,
		.tag       = hmac_sha384_tc1_tag,
		.tag_len   = 48,
		.source    = "RFC 4231 §4.2 TC1: HMAC-SHA-384(0b×20, \"Hi There\")",
	},
	{
		/* RFC 4231 §4.3 — TC2: key is 4 bytes, below FIPS 140-3 minimum */
		.key       = hmac_key_jefe,
		.key_len   = 4,
		.input     = hmac_input_jefe_what,
		.input_len = 28,
		.tag       = hmac_sha384_tc2_tag,
		.tag_len   = 48,
		.source    = "RFC 4231 §4.3 TC2: HMAC-SHA-384(\"Jefe\", \"what do ya want for nothing?\")",
		.fips_skip = 1,
	},
	{
		/* RFC 4231 §4.4 — TC3 */
		.key       = hmac_key_aa20,
		.key_len   = 20,
		.input     = hmac_input_dd50,
		.input_len = 50,
		.tag       = hmac_sha384_tc3_tag,
		.tag_len   = 48,
		.source    = "RFC 4231 §4.4 TC3: HMAC-SHA-384(AA×20, DD×50)",
	},
};

#define HMAC_SHA384_VECTOR_COUNT \
	((int)(sizeof(hmac_sha384_vectors) / sizeof(hmac_sha384_vectors[0])))

/* ── HMAC-SHA-512 vector table ───────────────────────────────────────────── */

static const struct hmac_vector hmac_sha512_vectors[] = {
	{
		/* RFC 4231 §4.2 — TC1 */
		.key       = hmac_key_0b20,
		.key_len   = 20,
		.input     = hmac_input_hi_there,
		.input_len = 8,
		.tag       = hmac_sha512_tc1_tag,
		.tag_len   = 64,
		.source    = "RFC 4231 §4.2 TC1: HMAC-SHA-512(0b×20, \"Hi There\")",
	},
	{
		/* RFC 4231 §4.3 — TC2: key is 4 bytes, below FIPS 140-3 minimum */
		.key       = hmac_key_jefe,
		.key_len   = 4,
		.input     = hmac_input_jefe_what,
		.input_len = 28,
		.tag       = hmac_sha512_tc2_tag,
		.tag_len   = 64,
		.source    = "RFC 4231 §4.3 TC2: HMAC-SHA-512(\"Jefe\", \"what do ya want for nothing?\")",
		.fips_skip = 1,
	},
};

#define HMAC_SHA512_VECTOR_COUNT \
	((int)(sizeof(hmac_sha512_vectors) / sizeof(hmac_sha512_vectors[0])))

#endif /* CRYPTO2DEV_VECTORS_HMAC_H */
